# Python
__pycache__/
*.py[cod]
*.pyo
.env
venv/
.venv/

# macOS
.DS_Store

# 一時ファイル
*.tmp
output_*.json

# キャッシュ
.cache/

# 注：以下はGitで管理する（全データの正規保管場所）
# watchlist/data/watchlist.json     → ウォッチリスト（Render APIが読み書き）
# portfolio-health/data/portfolio.json → ポートフォリオ（Render APIが読み書き）
# webapp/data/stocks/*.json         → 分析JSON（Claudeが生成・push）
# webapp/manifest.json              → 銘柄一覧（update_manifest.pyが更新）
